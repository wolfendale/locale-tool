# locale-tool


```zsh
  Usage: index <cmd> <files...>


  Commands:

    report <files...>
    merge [options] <files...>

  Options:

    -h, --help     output usage information
    -V, --version  output the version number
```
